#!/bin/bash
# Secrets managed by chezmoi + 1Password
# These values are pulled from 1Password on `chezmoi apply`

# Example: pulling a secret from 1Password (uncomment when op CLI is installed)
# export MY_API_KEY={{ `{{ onepasswordRead "op://Personal/MyAPI/credential" }}` }}

# Add your secrets here using the 1Password CLI reference format:
# {{ `{{ onepasswordRead "op://vault/item/field" }}` }}
