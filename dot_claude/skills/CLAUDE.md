# Claude Instructions for Skills Repo

## CRITICAL: Git Workflow Override

**Work directly on `main`.** No feature branches — override the global CLAUDE.md rule.

## CRITICAL: Always Commit and Push

After every change, immediately commit and push to origin/main. Do not wait for user confirmation to push.
